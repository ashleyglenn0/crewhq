import React from "react";
import { View, Text } from "react-native";

export default function AISchedulerTab() {
  return (
    <View>
      <Text>Automatically Schedule Shifts (coming from tab)</Text>
    </View>
  );
}